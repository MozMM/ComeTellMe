const { 
  sum, 
  alphabet, 
  angryWords, 
  sansPunctuation, 
  checkForAngryWords, 
  splitWordSets 
} = require('../utils/wordUtils');
// const jest = require('jest');

// const angryInputWithPunctuatuation = 'Never have I ever, been so mad!!'

// test('adds 1 + 2 to equal 3', () => {
//   expect(sum(1, 2)).toBe(3);
// });



describe('Punctuation remover function', () => {
  it('should run a test', () => {
    expect(true).toBeTruthy();
  });
  // it('it should return a copy of a lowercase word with only its alphabet characters, removing any others'), () => {
  //   expect(sansPunctuation('wut?!?!?!?')).toMatch(/wut/);
  // }
});



